{
  # Milestone 3 pins and imports the Disko module seam but deliberately declares
  # no device graph. The provider-qualified candidate disk and guarded
  # destructive installer are separate reviewed milestones.
}
