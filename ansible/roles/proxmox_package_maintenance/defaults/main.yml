---
# Choose exactly one scope. No implicit all-package operation.
# These established playbook inputs intentionally form the role's public interface.
proxmox_package_specs: [] # noqa: var-naming[no-role-prefix]
proxmox_package_dist_upgrade: false # noqa: var-naming[no-role-prefix]
# Metadata refresh is a separate reviewed write, never implied by package scope.
proxmox_package_refresh_metadata: false # noqa: var-naming[no-role-prefix]
