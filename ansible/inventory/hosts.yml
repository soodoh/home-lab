---
# Native SSH over the existing authenticated Tailscale connection.
# Deliberately separate from legacy docker_host/proxmox_host group variables.
all:
  children:
    home_lab:
      vars:
        ansible_connection: ssh
        ansible_python_interpreter: /usr/bin/python3
        ansible_become: true
        ansible_become_method: sudo
        ansible_ssh_common_args: >-
          -F /dev/null
          -o BatchMode=yes
          -o StrictHostKeyChecking=yes
          -o UpdateHostKeys=no
          -o GlobalKnownHostsFile=/dev/null
          -o UserKnownHostsFile=~/.ssh/known_hosts
          -o IdentityAgent=none
          -o IdentityFile=none
          -o IdentitiesOnly=yes
          -o PreferredAuthentications=none
          -o PubkeyAuthentication=no
          -o PasswordAuthentication=no
          -o KbdInteractiveAuthentication=no
          -o GSSAPIAuthentication=no
          -o ClearAllForwardings=yes
          -o PermitLocalCommand=no
          -o ConnectTimeout=10
          -o ServerAliveInterval=15
          -o ServerAliveCountMax=2
      hosts:
        docker-host:
          ansible_host: docker-host.tailea1a78.ts.net
          ansible_user: ansible-deploy
          ansible_ssh_extra_args: -o HostKeyAlias=docker-host
          update_policy_manage_apt: true
        proxmox:
          ansible_host: proxmox.tailea1a78.ts.net
          ansible_user: proxmox
          ansible_ssh_extra_args: -o HostKeyAlias=proxmox
