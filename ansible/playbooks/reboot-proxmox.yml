---
- name: Perform one explicitly approved native Proxmox reboot
  hosts: proxmox
  gather_facts: false
  serial: 1
  any_errors_fatal: true
  roles:
    - role: proxmox_observe
    - role: proxmox_package_observe
    - role: proxmox_reboot
