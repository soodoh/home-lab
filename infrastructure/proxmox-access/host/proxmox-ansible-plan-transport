#!/bin/sh
set -eu
if [ "$#" -ne 2 ] || [ "$1" != -c ] || [ "$2" != observe ] || [ -n "${SSH_ORIGINAL_COMMAND-}" ]; then
  echo "proxmox-ansible-plan-transport: fixed invocation rejected" >&2
  exit 64
fi
exec /usr/bin/sudo -n -- /usr/local/libexec/home-lab/proxmox-observer observe
