#!/bin/sh
set -eu
attempt=0
while :; do
  if /usr/local/libexec/home-lab/proxmox-firewall-transaction boot-config-recover; then
    exit 0
  fi
  attempt=$((attempt + 1))
  if [ "$attempt" -lt 30 ]; then
    /usr/bin/sleep 2
  else
    attempt=0
    /usr/bin/sleep 5
  fi
done
