#!/bin/bash
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
export HOME_LAB_FLATCAR_DISK_IMPORT_CONFIRMED=import-reviewed-vm-100-flatcar-scsi3
exec "$root/scripts/import-flatcar-disk"
