#!/bin/bash
set -euo pipefail

root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
export HOME_LAB_FLATCAR_FIRST_BOOT_CONFIRMED=diagnose-reviewed-vm-100-flatcar-vga
exec "$root/scripts/boot-flatcar-first"
