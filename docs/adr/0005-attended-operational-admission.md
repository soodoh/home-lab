# Attended operational planning and apply admission

- Status: Accepted
- Decision owner: home-lab operator
- Accepted by: explicit selection of “Rebaseline attended operations”
- Amends: [ADR 0001](0001-ansible-host-lifecycle.md) planning/apply assurance only; [ADR 0004](0004-operational-nix-retirement.md)'s existing-host completion boundary remains

## Rollout deferral for runtime retirement — owner decision 2026-09-11

The owner explicitly approved [ADR 0004](0004-operational-nix-retirement.md)'s narrower **existing-host Nix runtime retirement**, separate from adoption of this controller. For that finish line, defer the coherent four-asset rollout below, admission compatibility resolution, native qualification, new-source convergence/no-change and source-aligned no-op-plan certification. None is reported passed. The journal-presence read was never approved or run and is deferred; retain journals, locks and recovery records without disposition. Installed firewall watchdog/boot recovery uses a persistent operation mutex; its incompatibility with admission presence-refusal is not evidence of Nix execution and does not authorize removing the mutex or disabling recovery.

This is a rollout deferral, **not a controller guard waiver**: every future planning, verification or apply invocation must still satisfy all requirements below. No ordinary-observation substitution, legacy private-preparer `summary`, raw-Tofu/unguarded apply, manifest downgrade, v6 substitution or other executable bypass is admitted. Unknown operations and uncertain independent console/recovery custody remain relevant write blockers. Keep all unshipped candidate source and evidence; this decision authorizes no installation, helper execution or host read.

## Implementation location — local source integration

The complete accepted source snapshot is preserved on local, unpublished branch `wip/deferred-attended-controller-3491395-01`. It includes independent documentation, Docker safety and manual-update improvements as well as the coherent attended controller; it is not a controller-only patch. Local `main` integrates the dependency-closed non-controller work and this non-authorizing decision record, not the attended executable/contract/schema changes. Main's older v6 source remains deferred and unqualified too. The v7 behavior below describes the candidate branch, not main's implementation. Clean committed source is neither installed capability nor qualification, deployment or publication authority; all decisions and guards below remain unchanged.

## Controller decision (binding when invoked)

Replace unshipped v6 locked-snapshot prerequisites for **steady planning, verification and provider apply admission** with `attended-operational-v1`. Use the existing complete seventeen-domain Ansible audit and fixed `observe-admission` dispatch carrying the protocol4 observation in a distinct admission envelope, exact saved plans and existing native host transactions. This is an operational admission rebaseline, not an audit-only workaround. Desired state remains solely `infrastructure/contract/home-lab.yml`.

The operator must maintain a quiet window from collection through final verification: exclude other controllers, host/API writers, backup/recovery/package/VFIO operations and queued reboots, with an independent access/console route and applicable backup/recovery readiness. Only `observe-admission` samples existing owner markers and descriptor-lock listings before and after observation without creating locks. Shared `observe` keeps its original payload and transaction-owned lock semantics; its output cannot substitute for admission evidence. The controller holds its existing descriptor during plan/apply/verify. Samples, ordinary controller UTC and authenticated SSH are **not** `snapshot-exclusive-v1`, universal exclusion, replay-resistant host challenges or queued-reboot custody. Resolve unknown/partial operations before a new plan; never retry or clear ownership on connection loss.

Bind an explicitly attended artifact to reproducibly rendered neutral observer/protected-collector bytes and the current contract-derived specification. Preserve pre-v6 VFIO semantics by selecting the retained `nix/proxmox/vfio-recover.py` as the expected implementation; its policy is still generated from the current contract. This is not proof of installed VFIO identity. Do not use the retained private preparer's `summary`: its dispatch creates permanent `operation.lock`, incompatible with the historical activator's reboot checks. Do not change mutation participants or invoke the gated v6 installer. The absent neutral collector, changed observer, fixed plan transport's new `observe-admission` literal and narrowly enumerated ansible-plan sudo rule require separately approved coherent installation/rollback and native qualification. Pair-only installation is insufficient. These are read-only capability changes, not mutation-participant replacements.

Manifest v7 and attended evidence/artifact discriminators reject stale, mixed and differently authorized inputs. Keep clean source, source/artifact hashes, separate plan/apply credentials, confirmations, complete independent contract checks, protected facts, controller and native transaction locks, before-state rechecks and exact provider plans. Missing/unknown facts fail; no automatic installation, refresh or reboot. Packages, access/storage changes, reboots and recovery retain their separate transaction approvals. Low-risk Proxmox transactions still cover only repositories and chrony, not all-domain convergence. Executed no-change convergence is distinct from skipped apply.

VM100 `scsi3`, boot `scsi3;net0`, absent `scsi0`, protected storage/access, Compose current/previous artifacts and images, Restic and firewall recovery, `C.UTF-8` and disabled automatic module-dependency installation remain binding. Debian startup/recovery is unchanged. The v6 capability/qualification candidate stays deferred and gated, not silently admitted by attended evidence. Local source fixtures do not qualify Ansible/Linux/provider/host behavior or authorize rollout; the [checklist](../host-lifecycle-completion-plan.md) separates runtime retirement from these deferred controller gates.
