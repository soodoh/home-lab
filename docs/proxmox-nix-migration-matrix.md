# Proxmox Ansible-to-Nix ownership migration matrix

## Purpose and fixed boundaries

This matrix is the reviewed ownership ledger for replacing only the Proxmox host path with a Nix-built reconciler. It does not authorize implementation or live mutation by itself.

The migration preserves these boundaries:

- Proxmox VE remains the official Debian-based appliance. The host is not converted to NixOS and does not use `proxmox-nixos`.
- [`../infrastructure/contract/home-lab.yml`](../infrastructure/contract/home-lab.yml) remains the single authoritative desired-state contract. Nix consumes a validated deterministic projection; it does not copy contract values into a second model.
- OpenTofu continues to own VM 100, its recovery image, and PVE PCI/USB mapping resources. The Nix host reconciler may validate those objects but must not create, update, delete, import, or rewrite them.
- Ansible remains the owner of Arch guest configuration, Compose deployment, and application recovery. OpenTofu, not Ansible, owns VM 100. Shared Ansible roles remain while the guest and application paths use them.
- PVE cluster state under `/etc/pve` is reconciled only through supported `pveum`, `pvesh`, `pvesm`, or PVE API operations, never through generic file replacement or Nix-store symlinks.
- Nix runs only on the trusted controller. No Nix daemon, store, or package installation is added to the PVE host; the host receives ordinary root-owned versioned observer and activator helpers.
- Protected credentials and hardware identities are runtime inputs. They do not enter Git, Nix derivations, Nix store paths, Nix host plans, logs, fixtures, or committed evidence. Plans expose only redacted counts/booleans; protected keyed attestations, when required for preconditions, remain outside shareable evidence. Plaintext and stable unkeyed hashes of protected identifiers are prohibited.
- Completed migrations are archived as evidence and recovery notes. They are not reintroduced into steady state.
- Routine PVE UI use is read-only. Reviewed Git configuration, pinned Nix inputs, versioned upstream package expectations, protected identities, and backups are the reconstruction inputs.

## Current composition and target entry points

The current local bootstrap is `ansible/playbooks/proxmox-bootstrap.yml`; the current steady path is `ansible/playbooks/proxmox-site.yml`. Both load the contract and `ansible/group_vars/proxmox_hosts.yml`, acquire `apply_lock`, and compose the Proxmox-specific and shared roles. The bootstrap additionally requires local execution, physical-console/trusted-LAN gates, and an official PVE installation. The steady play adds firewall, health, and cleanup.

The eventual cutover interface is:

```sh
nix run --no-update-lock-file --no-write-lock-file 'path:./nix#proxmox-host' -- plan --repo-root "$PWD"
nix run --no-update-lock-file --no-write-lock-file 'path:./nix#proxmox-host' -- apply \
  --repo-root "$PWD" --plan-sha <reviewed-sha256> --approve-plan-sha <same-sha256>
```

The protocol-v4 `proxmox-host` app exposes strict `plan`, protected `prepare`, and exact-hash `apply` forms. The observer accepts `version`, `self-check`, and `observe`; the fixed private preparer accepts only `summary` and `prepare`; the bundle-specific activator accepts `version`, `self-check`, and bounded canonical `session` envelopes. Prepare and apply derive every path, host, and command and accept no caller-selected payload or helper. The layered console-only transport bootstrap runs after the retained local Ansible bootstrap and installs exact helper bytes, a root-only session key, protected runtime state, and a non-secret install manifest. Until that separately approved production mutation occurs, live planning remains blocked or apply fails `bootstrap-required`. Packages, PVE API state, OpenTofu-owned state, watchdog actions, and reboot remain closed. `tofu-apply` remains unrestricted only as an explicit pre-cutover Ansible compatibility risk. See [`proxmox-guarded-apply.md`](proxmox-guarded-apply.md).

The Nix host `plan` is non-mutating and secret-free. It binds the Git commit and tree, validated contract projection, `nix/flake.lock`, the bundle's canonical content SHA-256 (independent of its input-addressed Nix store path), normalized redacted observed-before-state digest, typed action preconditions and freshness limit, and plan hash. `apply` acquires both locks, re-observes every affected precondition, and fails on stale or changed state without deriving replacement actions or implicitly replanning. `verify` performs live health and drift checks and must be followed by an empty plan. Retained OpenTofu binary saved plans remain protected mode-0600 local artifacts and may encode sensitive provider inputs under their existing manifest, policy, and credential safeguards; the Nix host plan/evidence exclusion does not redefine that retained boundary.

All mutable domains share one root-owned non-store rollback area whose manifest is bound to the reviewed plan. It requires strict modes, symlink refusal, free-space/capacity checks, bounded retention, and explicit handling for sensitive access captures; successful cold-boot qualification either securely removes each capture or records an approved bounded preservation decision. Network, SSH, firewall, and Tailscale actions additionally prearm host-local timed rollback watchdogs before mutation and cancel them only after local and independent controller canaries pass.

## Ownership matrix

Classification terms may be combined: **steady**, **bootstrap**, **audit**, **destructive**, **reboot-bound**, **completed migration**, **secret lifecycle**, **OpenTofu-owned**, and **runtime/data state**.

| Behavior / current owner and evidence | Classification | New owner and activation mechanism | Rollback or recovery | Validation and qualification | Secret boundary |
| --- | --- | --- | --- | --- | --- |
| Exact-host, Debian bare-metal, hostname, `pveversion`, local-bootstrap, console/LAN, and explicit-apply assertions in `ansible/playbooks/proxmox-bootstrap.yml:2-38` and `ansible/playbooks/proxmox-site.yml:2-30` | Bootstrap; steady; safety gate | Nix-built bootstrap/host command preflight. It validates the target identity before rendering or applying any action. Apply records the exact gate acknowledgements without treating them as durable desired state. | Stop before mutation on mismatch. Recovery starts again from the official PVE installer with console and trusted-LAN access. | Unit-test every failed precondition. Live plan and apply must prove the intended node, PVE API, console, LAN rollback, clean commit, and absence of concurrent mutation. Cold boot repeats target identity and PVE version checks. | Gate acknowledgements are non-secret runtime inputs. Host fingerprints and bootstrap credentials remain protected outside the bundle and plan. |
| Host mutation lock in both Proxmox playbooks and `ansible/roles/apply_lock/tasks/main.yml:1-58` | Runtime state; safety gate | Nix uses the controller lock, a durable host ownership lock, and a no-follow root-owned descriptor-backed `flock` mutex per transition; process death/reboot releases serialization. Exact request results and release-pending/released terminal states are journaled for status/idempotent retry. | Verified rollback records release-pending, unlinks and directory-`fsync`s ownership, then records `released-recovered`; restoration failure retains ownership and diagnostics, while interrupted release is status-recoverable. Busy or unknown transitions never trigger concurrent rollback. | Tests cover live contention/crash release, lost responses, exact retries, terminal release windows, rollback failure, and retained evidence. Live qualification must repeat these cases. | Lock/journal metadata contains identifiers and hashes, never credential values. The shared Ansible `apply_lock` role remains for Arch/Compose users. |
| Contract projections and complete gate inventory in `ansible/group_vars/proxmox_hosts.yml`, `ansible/playbooks/proxmox-bootstrap.yml`, `ansible/playbooks/proxmox-site.yml`, Proxmox role defaults, and `recovery/extra-vars.example.yml:1-25` | Steady; bootstrap; secret lifecycle; safety policy | Before adding Nix behavior, move every non-secret Proxmox desired value that exists only in group vars/defaults/templates into `infrastructure/contract/home-lab.yml` and `schema.json`: exact direct/critical PVE/kernel/ZFS package versions, the full installed-manifest policy, services, Unix/human accounts and sudo, PVE users/roles/ACLs, SSH/firewall/Tailscale host preferences, signing-key URL/checksum, and file policies. Temporary Ansible derives from that contract and must remain a no-op. Nix then consumes a deterministic validated JSON projection; projections/manifests are generated and non-authoritative. Gates become typed actions/apply acknowledgements rather than duplicate desired state. | Revert the Git contract or reconciler commit; regenerate and review a new plan. Never edit generated output as desired state. | Schema validation, deterministic projection golden tests, unknown-field rejection, contract/projection hash binding, Ansible parity/no-op, and duplicate-state tests. | Protected ZFS, USB, Tailscale, SSH, and token values remain root-owned runtime inputs. Contract stores only their reference names; shareable plans contain redacted facts, not identifiers or stable hashes. |
| Canonical Debian, Proxmox, and Tailscale repositories; inactive `/etc/apt/sources.list`; historical stale-source/key cleanup in `ansible/roles/proxmox_host/tasks/main.yml` and defaults | Steady; completed stale-repository migration | Package/repository domain renders only canonical Deb822 files and keyring checksum expectations from the contract bundle, then runs `apt-get update`. Steady policy audits and reports unknown or archived legacy files; it never repeats historical deletion. A new deletion requires a separately reviewed migration action. | Restore captured pre-apply canonical files from the protected rollback area and rerun `apt-get update`. Console path remains available if package services are affected. | Plan inventories all active source files and keyring hashes, fails on unapproved sources, and checks required versions are available upstream. Apply validates exact canonical files and `apt-cache policy`; verify repeats both. Fresh-PVE bootstrap and cold boot must pass. | Repository metadata and key checksums are non-secret. No repository credentials are permitted in contract, bundle, or plan. |
| Required packages and services in `ansible/roles/proxmox_host/tasks/main.yml` and the contract-derived `proxmox_package_entries`, `proxmox_package_names`, and `proxmox_services` group vars | Steady; audit | Packages remain closed pending a sealed aggregate session. Per-service policies permit automatic guarded activation only for Chrony; SSH/Tailscale remain access-critical/watchdog-required and NFS remains data-critical/nonautomatic. | A package action records the prior version and whether it was installed; rollback uses an explicitly reviewed available upstream version or official recovery media. Services restore prior enablement/state where safe. No automatic distribution downgrade. | Planning fails clearly when any required exact version is unavailable. Tests cover epoch/version parsing, exact APT selection, service drift, and no accidental upgrade. Verify uses `dpkg-query`, `apt-cache policy`, and `systemctl`. Cold boot proves enabled services start. | No package cache, mirror, snapshot service, retained `.deb` cache, or private Debian/PVE mirror is introduced. External upstream availability is an accepted recovery dependency. |
| Tailscale signing key download checksum in `ansible/roles/proxmox_host/tasks/main.yml` | Steady; supply-chain control | Bundle records the upstream URL and SHA-256; package domain downloads to a protected temporary file, verifies the checksum, and installs an ordinary root-owned keyring file. | Restore the prior verified keyring file or reinstall it from the same checksum-bound upstream artifact. | Tests reject checksum mismatch, redirects to unapproved origins, or wrong mode/owner. Verify hashes the installed keyring. | Public signing material is non-secret. Download logs must not include unrelated environment values. |
| `tofu-plan` / `tofu-apply` Unix users, homes, account-specific authorized keys, and apply sudo policy in `ansible/roles/proxmox_host/tasks/main.yml` | Steady; bootstrap; secret lifecycle; access safety | The protocol-v4 contract gives `tofu-plan` only the exact observer sudo rule and forces every plan key to that command. `tofu-apply` intentionally retains unrestricted `NOPASSWD: ALL` for Ansible parity until the separately approved cutover. The target narrows apply to the fixed preparer, activator, and bounded canaries. Neither identity may execute arbitrary uploaded code. Public keys are loaded only during runtime activation from protected inputs. | Keep the active controller session and console open. Restore prior passwd/group/sudoers/authorized-key material from the plan-bound rollback area; validate with `visudo` before replacement. | Tests cover empty/distinct key rejection, account separation, password locks, modes, exact sudo command/argument boundaries, rejection of uploaded code, and no plan-user mutation. Live qualification proves both SSH identities independently before tightening access and after cold boot. | Public keys are sensitive operational inputs even though not private secrets; do not place controller-specific keys in the Nix store or Nix host plan. Private keys remain only on the protected controller. |
| Human `proxmox` account/group/home, password lock, no conventional authorized keys, and passwordless sudo in `ansible/roles/human_access/tasks/main.yml` as composed by both Proxmox plays | Steady; destructive access policy | Proxmox access domain reproduces only the Proxmox account contract. The shared Ansible role remains for Arch. Human access remains Tailscale-SSH-first, with console/LAN fallback required before changes. | Restore prior group/passwd/sudoers and authorized-key policy through console or trusted LAN. Never remove the last proven human path during the same unverified transaction. | Unit tests cover adoption versus creation, UID/GID/name mismatch refusal, password lock, supplementary groups, sudo validation, and access canaries. Live and post-cold-boot tests prove human Tailscale SSH and console fallback. | Tailscale identity policy and any emergency key material remain protected runtime/control-plane state. Plans show only account names and policy hashes. |
| PVE API users, roles, privileges, token existence, exact ACLs, and root-only escrow in `ansible/roles/proxmox_host/tasks/api.yml`, `api-check.yml`, `api-token.yml`, and `api-acl.yml` | Bootstrap; steady; destructive ACL reconciliation; secret lifecycle; OpenTofu prerequisite | PVE access domain uses `pveum`/`pvesh` to diff and reconcile named roles and ACLs. It creates tokens only when absent or during an explicitly selected rotation workflow. The one-time secret is captured from subprocess output directly into protected process memory and an atomically created mode-0600 root handoff; it is never operator-visible stdout or a bundle output. Failure paths redact diagnostics and destroy partial memory/files before retaining only non-secret status. | Before ACL mutation, record non-secret role/ACL state. Restore prior roles/ACLs with `pveum`. Token secrets cannot be recovered: revoke and rotate, then transfer new plan/apply capabilities through the protected channel. Retain console/root API recovery. | Tests cover exact set diffing, removal of unreviewed token ACLs, VM-scoped audit ACLs, role privilege ordering, token-present/no-escrow behavior, subprocess capture, failure cleanup, one-time handoff, and redaction. Live qualification proves authenticated plan/apply API operations and rotates tokens when required. | Token values exist only in protected memory, root-owned mode-0600 runtime files, or direct protected transfer. They never enter operator-visible stdout, logs, Nix host plans, derivations, store outputs, fixtures, or committed evidence. |
| Permanent management networking in `ansible/roles/proxmox_network/tasks/main.yml` and `ansible/roles/proxmox_network/templates/interfaces.j2` | Steady; destructive; network lockout risk | Network domain renders `/etc/network/interfaces` into the bundle, compares exact content/mode, requires console and trusted-LAN gates, validates with `ifquery`, prearms the host-local rollback watchdog, writes atomically, runs `ifreload -a`, and cancels rollback only after local plus controller canaries pass. | The watchdog restores the plan-bound previous file and runs `ifreload -a` on timeout or failed canary; console/trusted-LAN procedures remain mandatory because remote automation cannot prove console availability. | Tests cover deterministic rendering, interface/IP/MAC/bridge invariants, invalid config, watchdog timeout/cancel, canary failure, and rollback selection. Live qualification proves management IP, gateway, DNS, SSH, PVE UI/API, and VM bridge before and after cold boot. | Network desired state is non-secret. Protected host fingerprints and controller source identity stay outside the bundle. |
| Network snippet inventory and one-time single-file migration marker in `ansible/roles/proxmox_network/tasks/main.yml` | Completed migration; audit | Archive the reviewed snippet checksums and migration procedure. Steady Nix policy asserts that no active snippets exist and owns only the canonical main file; it does not carry forward migration flags or deletion behavior. | Recovery from a historical host uses the archived procedure after console review; a fresh official PVE install renders the canonical file directly. | Plan fails on any active snippet and reports path/hash without deleting it. Live and cold-boot verification assert the canonical file and empty active snippet set. | No secrets. Evidence records only file paths and hashes. |
| IOMMU command line, canonical VFIO modules and protected device-identity policy, modprobe files, exact legacy module-line/file absence, initramfs and boot refresh in `ansible/roles/proxmox_passthrough/tasks/main.yml` and handlers | Steady; audit; reboot-bound; destructive boot/device binding | Boot/passthrough domain renders ordinary boot/module files and treats the declared legacy paths as `audit-absence` expectations. Steady Nix planning reports an unexpected matching line or file without deleting it; any new deletion requires a separately reviewed activation. Temporary Ansible consumes the same records for compatibility, classifies changes as reboot-required, runs native boot refresh tools, and stops at the reboot boundary. | Restore captured boot/module files, rebuild initramfs and boot entries, select the known bootable kernel at console, and reboot. No NixOS-generation rollback is claimed. | Tests cover exact module/device sets, host iGPU exclusion, command selection, reboot classification, and rollback manifest. Live pre-reboot checks plus approved cold boot prove kernel command line and drivers. | PCI vendor/device/BDF identities and any stable hashes are protected runtime data. Shareable evidence exposes only booleans/counts. |
| Legacy host Coral/DKMS/package/udev cleanup in `ansible/roles/proxmox_passthrough/tasks/main.yml` | Completed migration; destructive; reboot-bound | Archive the completed transition and its recovery notes. Steady state only asserts prohibited packages/modules/rules are absent and the Coral is bound as specified; it does not repeat DKMS removal logic. Fresh official PVE bootstrap starts from the supported package set and never installs the legacy stack. | Archived recovery identifies the packages/rule/initramfs steps required to reverse the historical transition if hardware recovery demands it. | Plan reports prohibited artifacts as gated cleanup drift rather than silently deleting them. Current live and cold-boot verification checks absence and VFIO binding. | Hardware identity references and values remain protected runtime inputs; plans/evidence expose only booleans/counts. |
| Post-reboot PCI binding, prohibited module, and marker checks in `ansible/roles/proxmox_passthrough/tasks/main.yml` | Audit; reboot-bound evidence | Verification domain performs direct live assertions. Secret-free evidence binds commit, bundle hash, plan hash, boot ID, running kernel, and observed bindings; marker files are not treated as the source of truth. | Detection stops further domains. Use boot rollback and console procedure for incorrect binding. | Must pass after apply when no reboot is needed and after the approved cold boot. Evidence schema/tests reject stale commit, kernel, or boot ID. | Evidence contains only booleans/counts. PCI/device identities, driver-bound identity pairs, and stable unkeyed hashes remain protected runtime data. |
| Serial USB resolution, PVE mapping validation, legacy Home Assistant USB rule removal, and marker in `ansible/roles/proxmox_passthrough/tasks/main.yml` | Secret lifecycle; OpenTofu-owned validation; completed migration | Runtime observer resolves protected serials to unique physical paths. Nix verifies that OpenTofu-owned PVE mappings match; it never mutates mapping resources. Archive the legacy-rule removal. Steady state only asserts the legacy rule is absent and mappings are correct. | Restore the archived udev rule only as emergency recovery, or correct protected runtime inputs and run OpenTofu to repair mappings. | Tests cover no/duplicate serial matches, sysfs links, path normalization, mapping diff, and ownership collision. Live and cold boot prove Zigbee, Z-Wave, and Bluetooth mappings plus guest visibility. | USB serials and resolved physical paths remain root-owned runtime inputs. Shareable plans/evidence expose only redacted counts/booleans; any keyed precondition attestation remains protected and is never copied into evidence. |
| Protected hardware discovery in `scripts/collect-proxmox-protected-inputs:1-86` | Bootstrap; secret lifecycle; runtime identity | Retain the collector as the single runtime discovery boundary, or wrap it without changing its protected output contract. The controller-side bootstrap invokes it over console-backed trusted-LAN root access; Nix consumes only the non-secret success/count projection while OpenTofu and host observers receive protected values through their existing runtime channels. | Re-run discovery from the source serial inputs; never reconstruct identities from a plan or evidence. Stop if results differ from the reviewed physical inventory. | Tests preserve exact allowed input names, root ownership/modes, duplicate/missing-device rejection, and no output disclosure. Live bootstrap and cold boot revalidate the physical devices without publishing identifiers. | Games disk, ZFS GUID/members, USB serials, and resolved paths remain only in root-owned mode-0600 inputs; no plaintext or stable unkeyed identity hash enters Git, Nix, plans, logs, or evidence. |
| Protected ZFS GUID/member topology, import gating, health, resilver/removal/checksum assertions in `ansible/roles/proxmox_storage/tasks/main.yml` | Runtime/data state; audit; bootstrap/recovery; destructive import risk; secret lifecycle | Storage observer loads protected identities at runtime and validates exact pool GUID, member set, mirror topology, health, and activity. Bootstrap may import only the exact known GUID after console and explicit recovery gates. It never creates, formats, or overwrites a pool. | Export only when documented safe; retry exact-GUID import from console. Restore data from backups rather than synthesizing pool topology. Stop on degraded, resilvering, removal, missing member, or checksum regression. | Unit fixtures cover healthy/degraded/resilver/removal/missing-member/topology states. Live apply and cold boot verify `zpool status`, exact GUID/members, datasets, mounts, and backup health. | Pool GUID and all `/dev/disk/by-id` members are protected root runtime inputs, excluded from derivations, plans, logs, fixtures, and committed evidence. |
| ZFS userspace/kernel alignment and ARC/initramfs configuration in `ansible/roles/proxmox_storage/tasks/main.yml`, handlers, and `scripts/migrate-proxmox-zfs-stack` | Steady audit; reboot-bound; completed package transition | Package domain enforces versioned upstream ZFS/kernel expectations; boot/storage domain renders ARC settings and rebuilds initramfs. The previous one-time version migration script is archived after parity. Reboot remains a separately approved boundary. | Use console to boot the retained reviewed kernel, reinstall exact available Proxmox/ZFS packages, restore ARC config, rebuild initramfs/boot entries, and reboot. | Plan detects userspace/kernel mismatch and unavailable package versions. Live and cold-boot verification checks running kernel, module/userspace versions, ARC max, ZFS health, and boot synchronization. | No secret values in package or ARC policy; protected pool identities remain runtime-only. |
| PVE ZFS storage registration in `ansible/roles/proxmox_storage/tasks/main.yml` | Steady; PVE API state; runtime/data safety | Storage domain uses `pvesm status/add/set` to reconcile only the named registration and compatible fields. It does not create datasets or VM disks. | Restore the previous non-secret `pvesm` configuration. Removal is manual and separately reviewed because guests may reference the storage. | Tests cover absent/compatible/incompatible storage, field normalization, refusal to change type/pool destructively, and final diff. Live and cold boot prove registration, availability, and VM 100 storage health. | Storage names and mountpoints are non-secret; protected pool identity is checked separately at runtime. |
| NFS export, ZFS `sharenfs` retirement, guest/client canaries, and migration marker in `ansible/roles/proxmox_storage/tasks/main.yml` and `ansible/roles/proxmox_storage/templates/home-lab.exports.j2` | Steady export policy; completed migration; runtime/data state; destructive availability risk | Archive the old subnet-export-to-exact-client cutover and guest mutation procedure. Steady storage domain owns the exact ordinary export file, asserts ZFS `sharenfs=off`, runs `exportfs`, and validates the client-specific export without writing workload data. | Restore the prior export file/property from protected rollback capture and reload exports. Use console plus VM/client access to confirm service restoration. | Tests cover exact options/client, duplicate/conflicting exports, safe reload, and no-root-squash policy. Live and cold boot prove server export, VM 100 mount, source identity, and bounded read/write canary. | Export policy is non-secret. No workload data or backup content enters plans/evidence. |
| Dataset mount assertion in `ansible/roles/proxmox_storage/tasks/main.yml` | Audit; runtime/data state | Verification domain asserts the expected dataset is mounted at the expected path with ZFS type and does not create application data. | Stop and follow ZFS recovery; never mount an unidentified filesystem into the application path. | Live apply, final verify, and cold boot all check mount identity. | Protected member identities remain runtime-only. |
| PVE firewall policy and enablement in `ansible/roles/proxmox_firewall/tasks/main.yml:1-47` and `ansible/roles/proxmox_firewall/templates/cluster.fw.j2:1-12` | Steady; completed enablement migration; destructive network lockout | Firewall domain renders and diffs the reviewed policy but mutates PVE state only through supported PVE API/CLI operations. It requires console/LAN/Tailscale canaries and a prearmed host-local watchdog before changing rules or enablement; the watchdog is canceled only after backend status plus independent canaries pass. The one-time initial enablement migration is archived; later policy changes remain gated. | Watchdog/API recovery restores the prior plan-bound policy or disables the firewall on timeout; console then re-proves LAN and Tailscale access. | Tests cover normalized rules, default policy, required LAN/NFS/WireGuard/SSH/UI allowances, API/CLI rejection, watchdog timeout/cancel, invalid addresses, and canary failure. Live and cold boot prove firewall backend status and permitted/denied paths. | Firewall policy is non-secret. Controller identities and tokens remain protected. |
| Tailscale package/service, first enrollment, complete preference convergence, tag/IP health in `ansible/roles/tailscale/tasks/main.yml` as composed by Proxmox plays | Bootstrap; steady; secret lifecycle; management-plane safety | Proxmox host domain uses native `tailscale` and systemd. Enrollment is bootstrap-only and consumes a one-use auth key from a root-only runtime file. Preference/SSH changes prearm a host-local watchdog and are committed only after LAN plus tailnet canaries. The shared Ansible role remains for Arch. The separate OpenTofu Tailscale root remains sole owner of tailnet policy, grants, and SSH rules. | Watchdog restores recorded non-secret host preferences on timeout; console/LAN may run `tailscale down` or rotate/re-enroll with a new one-use key. Never reuse enrollment credentials or mutate control-plane policy from the host reconciler. | Tests cover uninitialized/running states, no-key steady plan, temp-file cleanup, exact preferences, watchdog timeout/cancel, OpenTofu ownership collision, and redaction. Live and cold boot prove tag, MagicDNS, direct SSH, API endpoints, and no advertised routes/DNS drift. | Auth key never enters environment dumps, argv, store, Nix host plan, or logs; it is loaded from a root-owned input and deleted immediately after use. OAuth/policy credentials remain controller-only. |
| OpenSSH host keys and managed daemon policy in `ansible/roles/ssh/tasks/main.yml` and handler as composed by Proxmox plays | Bootstrap; steady; destructive access policy | Proxmox SSH domain ensures native host keys, validates a rendered drop-in with `sshd -t`, requires proven alternate access, prearms the host-local rollback watchdog, atomically installs it, reloads `ssh.service`, and cancels rollback only after independent new-session canaries. The shared Ansible role remains for Arch. | Keep current sessions open; the watchdog restores the plan-bound prior drop-in and reloads on timeout, while console/LAN remains the recovery path. Do not tighten authentication in the same step that first establishes unverified access. | Tests cover exact `AllowUsers`, root policy, disabled password/keyboard auth, syntax failure, watchdog timeout/cancel, rollback, and key preservation. Live and cold boot prove both service identities and documented human path. | Host private keys remain host-generated and never enter the store or evidence. Controller private keys remain off-host. |
| Local PVE API, Tailscale, optional VM 100, ZFS, NFS health in `ansible/roles/proxmox_health/tasks/main.yml` | Audit; runtime/data state; OpenTofu-owned observation | Verification domain runs authenticated PVE API and native host checks. VM 100 is observed only; desired VM state remains OpenTofu-owned. | Detection fails the apply and preserves diagnostics/lock. Recover the failing native service, storage, access path, or OpenTofu resource through its owner. | Focused unit parsers plus live verify, empty plan, and complete post-cold-boot qualification. UI/API checks include authenticated API and trusted TLS/controller reachability, not loopback responsiveness alone. | API credential remains runtime-only and redacted; health evidence records status and identities, not token values or workload data. |
| Prohibited package/root-path removal, root shell reset, exact kernel/header retention and boot regeneration in `ansible/roles/proxmox_cleanup/tasks/main.yml:2-409` | Completed migration; destructive; reboot/boot-bound; steady audit | Archive historical package, root-environment, and old-kernel cleanup actions. Steady package/audit domains enforce the approved current manifest, root shell, preserved paths, exact running/retained kernel policy, and absence of archived artifacts. Any new deletion is a separately reviewed gated action, not a recurring broad sweep. | Use official repositories/recovery media to reinstall exact available packages and kernels; restore root-local data from backups; restore boot files and select a retained kernel from console. No retained `.deb` cache is created. | Tests cover dependency classification, exact kernel ownership, header matching, boot image/GRUB/proxmox-boot-tool checks, preserved-path refusal, and unknown drift. Live and cold boot prove the expected kernel and boot configuration. | Preserved token files are checked only for existence/owner/mode. Their contents never enter plan, store, log, or evidence. |
| Unknown manual-package and root-local-path audit in `ansible/roles/proxmox_cleanup/tasks/main.yml:410-461` | Steady audit | Package/file inventory domain deterministically classifies approved direct packages, dependency closure, archived removals, preserved roots, and unknown drift. Plan reports unknowns and refuses apply until the contract/policy is reviewed. | No automatic rollback or deletion: classify intentional state in Git or remove it through a separately reviewed plan. | Fixture tests cover package epochs/architectures, dependency closure, path traversal/symlinks, and deterministic normalization. Live final plan must contain no unknown drift. | File contents are not collected; only safe path/type/owner/mode/hash metadata for explicitly non-secret files is emitted. |
| VM/device resources in `infrastructure/tofu/proxmox/main.tf:1-197` and `hardware-mappings.tf:1-46`, Tailscale control-plane resources in `infrastructure/tofu/tailscale/main.tf`, and legacy/qualification tombstones | OpenTofu-owned; completed migration tombstone | OpenTofu remains sole owner of VM 100, image, PCI/USB mappings, PVE resources already modeled there, and tailnet policy/grants/SSH rules. Empty `proxmox-legacy` and qualification backends remain tombstones. Nix only validates collision-free ownership and live health. | Use OpenTofu saved-plan/state recovery and existing policy. Never use Nix apply, `qm`, generic `/etc/pve` edits, `state rm`, or import as a workaround. | Ownership-collision tests reject bundle actions for VM 100, mappings, or tailnet policy. Existing Tofu policy/provider tests remain mandatory. Live and cold boot prove VM 100 startup, protection, mappings, policy, and health. | OpenTofu binary plans, credentials, and protected disk/USB inputs retain their existing protected controller/runtime boundary and are not reclassified as shareable Nix host evidence. |

## Package and reproducibility policy

The dedicated sanitized flake at `nix/flake.nix` pins the controller toolchain through `nix/flake.lock` to the supported `nixos-26.05` stable branch; it does **not** preserve external Debian, Proxmox, kernel, ZFS, firmware, or Tailscale packages. Run the examples from the repository root with an explicit path flake URL: `nix flake check 'path:./nix'` and `nix build 'path:./nix#proxmox-host-bundle'`. The `path:` prefix is mandatory because Git-aware resolution of a nested directory can archive the repository root. There is intentionally no repository-root flake. The sanitized source contains only the canonical projection and closed schemas, hash-bound package manifest and schema, bundle builder, fixed helper templates, guarded controller, fixture, and flake files. The builder renders both helpers from tracked templates plus validated bundle-specific catalogs; neither helper accepts a caller-supplied source or helper CLI input. The authoritative contract declares selected exact versions, semantic critical-package roles, prohibited packages, architecture, and the path and SHA-256 of the canonical expected installed manifest. The hash-bound manifest is a generated expected-state and solver-closure artifact, not an independently edited second desired-state list. Its timestamp-free, host-identity-free provenance binds the SHA-256 and installed-record count of a `dpkg` status TSV to the SHA-256 and normalized transitions of the reviewed joint APT simulation. Contract projection and bundle generation must therefore record:

1. repository file paths, types, URIs, suites, components, and signing-key paths;
2. approved signing-key download checksums where the key is not supplied by a pinned Debian/PVE package;
3. exact versions for every directly selected host package, including the reviewed PVE kernel/header and relevant ZFS/PVE package set;
4. the normalized full expected installed-package manifest used as the audited solver-result/dependency-closure gate; and
5. the expected native service set.

The observer and guarded apply do not refresh APT metadata or invoke an APT solver. Package drift is one complete-installed-map blocker, never per-package activation. It reads `dpkg-query` status-bearing records, retains exactly records whose second Status-Abbrev character is `i`, and compares canonical binary package name and version to the manifest. Architecture is parsed for record integrity but is not forced to `amd64` per record: `Architecture: all` and valid multiarch binary names remain representable. A later protected package milestone must separately implement and review authenticated metadata preparation, exact-version availability checks, and the joint no-remove/no-recommends/no-downgrade simulation described by the target policy; none is claimed by guarded apply.

A package-policy update starts with a fresh status-bearing inventory such as `${db:Status-Abbrev}\t${binary:Package}\t${Architecture}\t${Version}` and a saved joint `apt-get --simulate` result for the reviewed exact selection. Regenerate with `scripts/render-proxmox-package-manifest --inventory <status.tsv> --simulation <apt-simulation.log> --architecture amd64 --output nix/proxmox/package-manifest.json`, review every normalized transition and the final complete set, update selected versions and the manifest SHA-256 in the contract, and run the contract/package tests. Every status whose second Status-Abbrev character is `i` is installed inventory (including held or reinstreq selections); states such as `rc` are excluded. Neither the manifest nor its provenance includes timestamps or host identity. The renderer accepts only observed inventory plus simulation inputs; do not hand-edit the manifest or update its hash without regenerating and reviewing both inputs.

The project deliberately adds no APT mirror, snapshot service, private package cache, or retained `.deb` recovery cache. A future upstream garbage-collection event may require a normal reviewed contract/package-version update before recovery; that residual dependency is accepted and must be reported, never hidden behind a mutable “latest” install.

## Completed migrations to archive

These transitions remain useful evidence but are not steady desired-state procedures:

| Completed transition | Current evidence | Steady replacement |
| --- | --- | --- |
| Duplicate/stale APT definitions and orphaned signing keys | `ansible/roles/proxmox_host/tasks/main.yml` defaults and cleanup tasks | Assert only canonical active source files/keyrings; report unknowns without broad deletion. |
| Network snippets to one canonical `/etc/network/interfaces` | `ansible/roles/proxmox_network/tasks/main.yml` reviewed checksums and marker | Own the canonical file and fail on active snippets. |
| Legacy host Coral/Gasket DKMS/packages/udev rule removal | `ansible/roles/proxmox_passthrough/tasks/main.yml` | Assert prohibited host packages/modules/rule are absent and VFIO binding is correct. |
| Legacy Home Assistant USB udev rule to serial-driven PVE mappings | `ansible/roles/proxmox_passthrough/tasks/main.yml` | Validate protected serial resolution and OpenTofu-owned mappings; assert the rule is absent. |
| Raw VM 100 PCI/USB attachments to managed mappings | `docs/proxmox-appliance-maintenance.md:28-44`, including the failed-token-apply stop rule, reviewed root `qm set`, and required fresh no-op plan | OpenTofu owns managed mappings and VM references; never replay the exceptional root transition or stale saved plan. |
| Custom ROM and stale local-media retirement | `scripts/inventory-proxmox-local-artifacts:1-68` and `docs/proxmox-local-artifact-cleanup.md` | Validate references/absence only; retain the inventory procedure as archived evidence tooling, not steady deletion. |
| ZFS/kernel package alignment transition | `scripts/migrate-proxmox-zfs-stack` and storage/passthrough post-reboot markers | Exact package availability/version and runtime compatibility checks. |
| NFS subnet/ZFS export to exact VM export | `ansible/roles/proxmox_storage/tasks/main.yml` plus the SMART/scrub, checkpoint-resume, VM 100 guest-agent ownership, existing mount, and client read/write evidence in `docs/proxmox-appliance-maintenance.md:12-26,47-57` | Own the exact ordinary export and validate the client mount; retain the full pre-cutover evidence as archive, not recurring migration gates. |
| Initial PVE firewall enablement | `ansible/roles/proxmox_firewall/tasks/main.yml` | Own policy and enabled state; gate every lockout-risk policy change. |
| Old kernel/header pruning and root workstation-environment cleanup | `ansible/roles/proxmox_cleanup/tasks/main.yml` | Exact kernel/package/path audit; new deletions require a new reviewed migration. |
| CT 101/gateway retirement and disposable LXC qualification | Empty OpenTofu tombstones and tracked qualification evidence | Preserve tombstones/evidence; never recreate them in bootstrap or recovery. |

Archived notes must identify original evidence, final expected state, reversal/recovery guidance, and why the transition is excluded from steady activation. Marker files may be observed as historical evidence but never outrank the contract, current live facts, commit, bundle, and boot identity.

## Controller and recovery cutover seams

### Existing calls to replace

`scripts/reconcile-infrastructure` currently owns three Proxmox Ansible seams:

- `verify_all` requires a Proxmox Ansible no-op at `scripts/reconcile-infrastructure:820-849`;
- recovery apply pings the Proxmox inventory and runs `ansible/playbooks/proxmox-site.yml` before the Proxmox OpenTofu root at `scripts/reconcile-infrastructure:887-903`; and
- steady apply applies `proxmox-legacy`/`proxmox` before `ansible/playbooks/proxmox-site.yml` at `scripts/reconcile-infrastructure:904-911`.

`scripts/bootstrap-proxmox-host:1-95` creates a temporary hash-locked Ansible environment and runs `ansible/playbooks/proxmox-bootstrap.yml` locally.

### Required staged cutover

1. Expand `infrastructure/contract/home-lab.yml` and `schema.json` with every non-secret Ansible-only Proxmox desired value named in the matrix. Make temporary Ansible derive from the contract, run schema/semantic tests, and require the live Proxmox Ansible plan to remain a no-op before adding Nix behavior.
2. Add the pinned controller flake, deterministic validated projection, canonical content-hashed bundle, generated full package manifest, fixed helper foundation, and tests without routing production to them. No Nix component is installed on PVE.
3. Bootstrap the fixed observer/activator from the trusted controller over console-backed trusted-LAN root access, then prove helper version/protocol, bootstrap `verify`, and an empty bootstrap plan. Narrow plan/apply sudo only after both identities and rollback are proven.
4. Run the Nix plan in audit/shadow mode while the existing Proxmox Ansible check is still the production fallback. Normalize and compare findings by domain; explain every difference.
5. Bind the Nix bundle, observed-before-state, action preconditions, freshness, and Proxmox plan hashes into the controller manifest. Preserve both locks, clean-commit requirement, capability separation, interactive confirmation, OpenTofu saved-plan semantics, Arch/Compose sequencing, and post-apply verification.
6. Cut steady and recovery host convergence to the exact reviewed Nix plan. Recovery first applies the retained Tailscale OpenTofu control-plane root so required policy/grants/SSH rules exist, then runs console-backed Nix bootstrap/host work, then applies the Proxmox OpenTofu root for VM 100/mappings; Arch/Compose Ansible recovery continues afterward.
7. Live-apply the exact pushed `origin/main` cutover revision with console and trusted-LAN rollback, verify every domain, require an empty Proxmox plan, perform an explicitly approved cold reboot, and repeat complete verification.
8. Only after step 7 succeeds may the Proxmox Ansible playbooks, group vars/inventory membership, Proxmox-only roles, bootstrap wrapper behavior, controller calls, and stale documentation be removed. Shared `apply_lock`, `human_access`, `ssh`, and `tailscale` roles remain because Arch/Compose playbooks still use them.
9. Push the removal commits and verify the exact final `origin/main` revision live. The final Proxmox plan must remain empty and no Proxmox bootstrap, plan, apply, audit, verify, or recovery path may invoke Ansible.

Intermediate commits must keep the current production path functional until the single cutover commit. Audit parity is temporary and must not become permanent dual ownership.

## Contract/schema prerequisite completion and phase boundary

The contract/schema completion prerequisite is now implemented. `infrastructure/contract/home-lab.yml` and its closed schema define the remaining ordinary Proxmox managed-file paths and metadata plus their semantic inputs: APT source/keyring policy, enabled/started native services, networking, GRUB/VFIO, ZFS ARC/mirror topology/dataset sharing, NFS exports, SSH and account access metadata, Tailscale service/enrollment intent, and expected local health states. Temporary Ansible consumes these values; shared SSH, human-access, and Tailscale roles receive Proxmox-specific values explicitly so their Arch defaults and rendered content remain unchanged.

Every projection record has a structurally accurate machine-readable class: `managed-file` and `managed-directory` carry complete materialization policy, `managed-file-metadata` carries shared metadata without pretending to be a path-bearing file, and `audit-absence` carries an absence expectation rather than generic materialization. Steady Nix planning audits `audit-absence` drift; deletion requires a separately reviewed activation. Host private-key sentinels, SSH directories and authorized-key destinations, and token escrow use `runtime-protected-file` or `runtime-protected-directory`, `projectable: false`, and `materialization: metadata-only`; their contents can never be generic bundle outputs. PVE firewall state is `api-owned`, explicitly non-projectable, and classified `pve-api` for both ownership and activation. Its `/etc/pve/firewall/cluster.fw` compatibility path remains available only to temporary Ansible and is not an ordinary managed-file policy; semantic validation rejects any ordinary managed path at or below `/etc/pve`. PVE accounts, ACLs, tokens, and storage registration likewise remain PVE API/CLI domains. Protected key, token, USB-port, ZFS, and disk values remain runtime inputs represented only by reference names.

Staged-cutover prerequisites 1, the controller foundation, and protocol-v4 guarded controller and protected-bootstrap implementation are complete at the repository boundary. The `nix/`-rooted, `nixos-26.05`-pinned sanitized flake supports the aarch64-darwin controller plus x86_64-linux and aarch64-linux tooling evaluation. It consumes the tracked, schema-closed canonical projection and builds a deterministic bundle containing that projection, the current exact 1,353-package manifest, rendered non-protected ordinary files and audit expectations, protocol/metadata bindings, a fixed redacting observer, and a bundle-specific guarded activator. Package count is derived from and checked against the hash-bound manifest and its provenance rather than maintained as an implementation constant. The canonical bundle-content SHA-256 is emitted beside, not inside, the hashed tree. Validation archives and scans the exact sanitized source plus direct derivation source inputs, bundle output, and output closure. Nix remains controller-only.

The step-4 controller lane remains separate from cutover. The reviewed console-backed helper bootstrap is installed and verified, and the tracked lane is now `shadow-required`, so `local-controller plan` captures fixed sequential Ansible and Nix observations as canonical local evidence. That evidence binds one way to the unchanged version-3 controller manifest and exact Git/bundle/plan identities; blocked output is audit evidence rather than qualification, and apply never consumes it. Step-5 manifest binding, freshness, lock representation, private approval, recovery sequencing, and all Nix authority remain deferred.

The allowlist deliberately excludes runtime-protected records, all secret-reference names and values, token and authorized-key paths, PVE compatibility paths, controller/API locations, hardware identities, and cleanup/migration/approval state. Hardware-bound content such as the VFIO PCI ID line is therefore not rendered by the bundle even though its destination remains ordinary host policy. Before console bootstrap, the observer reports protected hardware and authorized-access verification as unavailable and planning adds deterministic blockers. After the exact protocol-v4 preparer is installed, it privately proves five independent access checks and exact disk/GUID/six-mirror/USB mapping checks, returning only counts/booleans; public PVE role/ACL intent remains a separate observer domain. Its local PVE process check is not the later authenticated trusted-controller canary. PVE firewall and storage intent remains semantic API intent. Guarded plan, private preparation, apply, console install/verify/recover, protected refresh, and key rotation are implemented at the repository boundary but are not routed into production; temporary Ansible remains authoritative until live shadow qualification.

## Live and cold-boot qualification gate

Before cutover, capture an Ansible no-op baseline and a Nix audit plan at the same committed revision and live state. The cutover apply requires explicit operator approval, physical console, tested trusted-LAN rollback, current backups, healthy ZFS, VM 100 protection/state confirmation, no other mutation, and exact commit/tree, `nix/flake.lock`, contract/projection, canonical bundle-content, redacted observed-state, and plan hashes. After both locks are acquired, every affected precondition is re-observed within the plan freshness limit; any mismatch stops without replanning.

After apply and again after a cold boot, verify at minimum:

- management bridge/address, gateway/DNS, LAN rollback, Tailscale, both SSH service identities, and human access;
- PVE UI and authenticated API, roles, ACLs, and token rotation/transfer state;
- exact repositories, signing-key hashes, package versions, running/retained kernel, initramfs, GRUB/proxmox-boot-tool, and native services;
- ZFS pool GUID/topology/health, datasets/mounts, PVE storage registration, NFS export, VM mount, and bounded read/write canary;
- PVE firewall state and permitted/denied paths;
- IOMMU/VFIO, Coral/GPU/audio bindings, host iGPU binding, USB mappings, and absence of prohibited legacy modules/rules;
- VM 100 startup, protection, OpenTofu no-op, workload/application health, and backup freshness;
- reconciler lock cleanup/failed-apply diagnostics; and
- `verify` success followed by a deterministic empty `plan` for the exact deployed commit and bundle.

Evidence is secret-free and schema-validated. It records the commit, contract hash, bundle hash, plan hash, host identity, before/after boot IDs, cold-boot fact, running kernel, domain outcomes, and timestamps. It does not record tokens, SSH keys, hardware serials/by-id paths, pool GUIDs, backup contents, or protected controller locations.

## Removal gate

**Proxmox Ansible removal is prohibited until the exact pushed Nix cutover revision has succeeded live, the final pre-removal plan is empty, a cold reboot has completed, all post-boot checks pass, and rollback access remains proven.**

After that gate, removal is limited to Proxmox-specific usage:

- remove `ansible/playbooks/proxmox-bootstrap.yml` and `ansible/playbooks/proxmox-site.yml`;
- remove `ansible/group_vars/proxmox_hosts.yml`, `ansible/inventory/proxmox-local.yml`, and all `ansible/roles/proxmox_*` roles/templates/handlers; remove the `proxmox_hosts` group and its credentials from mixed `ansible/inventory/infrastructure.yml` while retaining the Docker-host group;
- remove Proxmox calls/options/normalization from `scripts/reconcile-infrastructure`, `scripts/bootstrap-proxmox-host`, recovery examples, tests, and docs;
- retain Ansible controller dependencies and the shared `apply_lock`, `human_access`, `ssh`, and `tailscale` roles while Arch or Compose references remain; and
- retain OpenTofu Proxmox roots, policy, provider locks, VM 100 resources, mapping resources, tombstones, and qualification evidence.

The removal itself must be granular, committed, pushed, and then verified against the exact final `origin/main` revision. A stale Proxmox Ansible reference or non-empty final plan keeps the migration incomplete.

## Residual risks

- Exact Debian/PVE/Tailscale versions can disappear from upstream repositories; `nix/flake.lock` cannot recover them.
- Debian/PVE activation is not a NixOS generation and has no atomic whole-host rollback. Each domain needs explicit pre-apply capture and native recovery.
- Network, SSH, Tailscale, and firewall changes can remove remote access despite local validation; console and trusted-LAN rollback remain mandatory.
- Kernel, initramfs, boot, ZFS, and VFIO changes may fail only after reboot; cold-boot qualification is therefore a release gate, not optional evidence.
- PVE token values are one-time secrets. Missing escrow requires rotation, not attempted reconstruction.
- ZFS/data state and VM 100 are validated across ownership boundaries; the Nix reconciler must fail closed rather than taking ownership when another owner is unhealthy.
- Upstream package, Tailscale control-plane, Git remote, and official PVE installer availability remain external recovery dependencies.
