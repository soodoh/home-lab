# Ansible difference register

Allowed decisions are **Adopt current**, **Change current**, **Temporarily ignore**, and
**Block pending investigation**. Nothing in this register authorizes an apply.

| Item | Current | Desired | Decision | Apply phase | Rollback | Status |
|---|---|---|---|---|---|---|
| Gluetun health | Running, healthy | Remain healthy before and after Compose adoption | Adopt current | Revalidate at every cutover and apply | No automatic rollback; preserve current container and volume | Healthy after approved restart; baseline check passes |
| Seerr health | Running, healthy | Remain healthy before and after Compose adoption | Adopt current | Revalidate at every cutover and apply | No automatic rollback; preserve current container and volume | Healthy after approved restart; baseline check passes |
| Legacy volume `happier-data` | Present as `docker-compose_happier-data`; undeclared | Preserve indefinitely until ownership/data is investigated | Adopt current | Audit only; no removal phase | Never delete, rename, prune, or recreate | Preserved |
| Legacy volume `nzbget-data` | Present as `docker-compose_nzbget-data`; undeclared | Preserve indefinitely until ownership/data is investigated | Adopt current | Audit only; no removal phase | Never delete, rename, prune, or recreate | Preserved |
| Legacy volume `nzbhydra2-data` | Present as `docker-compose_nzbhydra2-data`; undeclared | Preserve indefinitely until ownership/data is investigated | Adopt current | Audit only; no removal phase | Never delete, rename, prune, or recreate | Preserved |
| Kernel and reboot | Running kernel and installed kernel/headers all `7.1.5-arch1-2`; boot artifacts validated | Preserve aligned kernel state; future reboots remain separate maintenance | Adopt current | Phase 4b maintenance complete | Boot prior/fallback entry from serial console only under a fresh recovery plan | Graceful reboot succeeded; matching module tree and `xt_mark` verified |
| Docker daemon version | Package, client, and daemon all `29.6.2` | Preserve aligned version without unplanned restarts/upgrades | Adopt current | Phase 4b maintenance complete | Use serial/LAN/gateway recovery; no automatic daemon restart | Reboot resolved prior daemon skew; 41 Compose services and 33 volumes verified |
| Production environment | Root-owned `/etc/docker-compose/production.env`, mode `0600`, reconstructed from SOPS | Keep the active and one previous environment outside Git and host checkouts | Adopt current | Stable deployment and every audit | Swap only with the hash-matched previous artifact through guarded rollback; never display contents | Active audit asserts `root:root 0600`; legacy plaintext checkout environment is retired |
| `/mnt/storage` | Active NFSv4 mount; fstab source uses NFS | Preserve exact current entry; assertions only initially | Adopt current | Phase 3 `storage` check; management-plane change only if later approved | Console/LAN recovery; no automatic fstab edit or remount | Check passed with zero change |
| `/mnt/games` | Active ext4 `/dev/sdb1`, `defaults,noatime` in fstab | Preserve exact current entry; assertions only initially | Adopt current | Phase 3 `storage` check; management-plane change only if later approved | Console/LAN recovery; never format or recreate filesystem | Check passed with zero change |
| Root Docker-prune cron | Root-owned mode `0600` crontab exists; content unread without privilege | Inspect exact content before deciding whether Ansible manages it | Block pending investigation | Before Phase 6 `maintenance` | Keep existing crontab untouched until captured and approved | Metadata assertion only; management blocked |
| Compose image references | All 41 services use readable upstream tags plus exact digests | Keep every tag/digest pair immutable until a reviewed Renovate update | Change current | Local validation and deployment | Restore the prior reviewed tag/digest pair; never auto-pull | Completed; `scripts/check-compose-image-pins.py` verifies the contract |
| Gluetun-dependent config hashes | Eleven namespace dependents were recreated only because the Gluetun container identity changed | Continue representing namespace sharing as `network_mode: service:gluetun` in Compose | Change current | Initial Compose cutover complete | Guarded previous-artifact rollback recreates only the deterministic reviewed set; no automatic rollback | Gluetun, Seerr, and all 41 services passed post-cutover checks |
| Tailscale gateway LXC | Protected unprivileged CT 101 runs Debian 13.6 at `192.168.0.122`; healthy `tag:infra-router` at `100.67.24.7`; exactly two `/32` routes operational; PVE TCP/8006 and Docker TCP/22 allowed; PVE TCP/22 denied | Preserve as independent bootstrap/recovery infrastructure; reconfirm serial/LAN recovery before direct Docker enrollment | Adopt current | Phase 4a complete; Phase 4b prerequisite | Keep CT protected and outside workload state; route unapproval/stop/delete requires fresh plan | Gateway routing verified with Mac `utun13`, HTTP/TCP tests, and firewall counters; 8 unrelated updates excluded |
| Headscale | Not deployed | Use hosted Tailscale control plane; do not add a same-failure-domain Headscale dependency | Adopt current | Architecture decision | Revisit only with an external, publicly reachable, independently recovered control-plane design | Rejected for current architecture |
| Docker Tailscale | `tailscale` `1.98.10-1` healthy as `tag:docker-host` at `100.111.210.72`; no routes/DNS acceptance; Tailscale SSH works as `ansible-deploy`; netfilter on | Preserve direct Tailscale SSH management path and least-privilege tag/policy | Adopt current | Phase 4b complete | Preserve LAN SSH, gateway route, and Proxmox serial console; down/disable/removal requires fresh plan | Kernel reboot restored `xt_mark`; backend Running with empty health; direct ping/SSH/sudo verified |
| Deployment user | `ansible-deploy` has locked password, private primary group only, sudoers `0440 root:root`, and validated passwordless sudo | Preserve dedicated unattended Ansible identity outside docker/supplementary groups | Adopt current | Phase 4b complete | Do not remove user/sudoers until another recovery path is confirmed | Local and Tailscale SSH identity/sudo tests passed; bootstrap check `changed=0` |
| Local plan user | Locked `ansible-plan` has no Docker group or broad sudo and one fixed read-only helper | Preserve a plan identity that cannot reach privileged deployment access before approval | Adopt current | Trusted-controller plan path | Remove only after replacing its read-only checks | Boundary remains read-only |
| Tailnet policy | Gateway and Docker identities remain; gateway stage is detached | Remove all hosted-controller tags, grants, tests, and federated identities while preserving owner/admin direct access | Change current | Exact `tailscale-controller-retirement` operation | Retain LAN/OpenSSH/gateway/serial recovery; reintroduction requires a new reviewed design | Policy-inspected retirement pending |
| Ansible availability | `ansible` `14.2.0-1`, core `2.21.2`; bootstrap/audit/site checks all `changed=0` | Retain for local and direct-Tailscale management | Adopt current | Phase 4 complete | Repository scaffold and recovery paths remain usable if future changes fail | Bootstrap `ok=13`, audit `ok=45`, site `ok=33`; all zero change |
| Local encrypted backups | 7 encrypted files observed; latest selected archive was 46,229,834,775 bytes | Preserve freshness and repeat the isolated recovery procedure periodically | Adopt current | Revalidate before stateful adoption | Never restore over production paths or volumes | External-key decrypt, full archive scan, and Vaultwarden SQLite integrity passed in 679 seconds |
| Remote backups | Weekly encrypted backup and prune completed; one 46,308,530,007-byte current S3 object independently listed | Preserve the 14-day retention path and verify future expiration behavior | Adopt current | Revalidate before stateful adoption | Keep local encrypted copies; no automated deletion | Current/version metadata and in-window retention verified; full expiration cycle remains a documented limitation |
| Wolf host files | 7 active-artifact/host file pairs match byte for byte | Preserve matching content with zero diff | Adopt current | Active artifact audit and guarded host-file apply | Restore captured matching files if a later approved change fails | Comparisons resolve `/srv/docker-compose/current`, not a host checkout |
| Compose service/volume baseline | 41 services running; 30 declared volumes; 33 project volumes including legacy | Preserve counts through repository-driven deployment | Adopt current | Preflight, cutover, and every guarded deployment | Stop on mismatch; never prune/recreate volumes | Initial cutover and Renovate canary post-audits both passed with zero change |
| Bind mounts and devices | 29 unique running bind sources and 8 unique running device sources exist | Refuse future apply if any are missing | Adopt current | Audit and Phase 3 hardware check; preflight before Phase 8 | Stop without apply; no filesystem/device creation | Checks passed with zero change |
| Controller execution | GitHub Actions is retired; GitHub is only a protected Git remote | Use the trusted MacBook, any clean committed revision, separate credentials, exact saved plans, and single-use local approvals | Change current | Local controller migration | Preserve Mac/LAN/gateway/serial recovery | Controller implemented; Roles Anywhere bootstrap pending apply |
| Compose deployment automation | Active and prior immutable artifacts are retained; no host Git checkout | Preserve exact-artifact staging, host locks, health checks, and zero-change post-audit under manual control | Adopt current | Local controller steady operation | Use the prior artifact only through separately reviewed recovery | No automatic apply or rollback |
| OpenTofu/Proxmox import | Not configured | Defer workload/VM import until host convergence; later isolate gateway CT 101 in protected bootstrap state or keep manual | Temporarily ignore | After Phase 9 readiness | Main workload state must never destroy its own gateway access path | Deferred |
| Apply safety guard | Normal apply requires one operation-specific local approval and the persistent host mutation lock | Fail closed on concurrent or abandoned convergence and preserve exact-plan consumption | Adopt current | Every trusted-controller apply | Successful runs remove locks; failures retain evidence for manual review | Apply never replans |

The eleven Gluetun namespace dependents observed with config-hash differences are: Seerr, Tachidesk,
Caro Tachidesk, FlareSolverr, Prowlarr, Sonarr, Radarr, Radarr 4K, Bookshelf, qBittorrent, and SABnzbd.
